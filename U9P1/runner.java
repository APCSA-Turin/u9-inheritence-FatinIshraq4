/*
public class runner {
    public static void main(String[] args) {
        Firetruck firetruck1 = new Firetruck("Truck1", 8, false, 0);

        //Vehicle methods
        firetruck1.getName();
        firetruck1.getWheels();
        firetruck1.move(10);
        firetruck1.turn();
        firetruck1.brake();

        //Firetruck methods
        firetruck1.activateSiren();
        firetruck1.deactivateSiren();
        System.out.println(firetruck1.getSiren());
        firetruck1.extendLadder(78);
        firetruck1.retractLadder(14);
        System.err.println(firetruck1.getLadderLength());
    }
}
    */
